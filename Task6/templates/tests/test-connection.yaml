apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Task6.fullname" . }}-test-connection"
  labels:
    {{- include "Task6.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Task6.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
